@model List<MilkyProject.WebUI.Dtos.TeamDtos.ResultTeamDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Default/_AdminLayout.cshtml";
}

<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title">Gruplar </h3>

        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Gruplar</h4>
                    <p class="card-description">
                        Aşagıdaki tablodan  grupların bilgilerini görüntüleyebilir,uzerinde işlem yapabilirsiniz.
                    </p>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>#</th>

                                    <th>Adı Soyadı</th>
                                    <th>Resim</th>
                                    <th>Meslek</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>

                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model)
                                {

                                    <tr>

                                        <td>@item.teamID</td>
                                        <td>@item.nameSurname</td>
                                        <td><img alt="Image placeholder" src="@item.imageUrl"></td>
                                        <td>@item.job</td>


                                        <td><a href="/Admin/Team/DeleteTeam/@item.teamID" class="btn btn-danger">Sil</a></td>
                                        <td><a href="/Admin/Team/UpdateTeam/@item.teamID" class="btn btn-success">Güncelle</a></td>

                                    </tr>

                                }



                            </tbody>
                        </table>
                        <a href="/Admin/Team/CreateTeam" class="btn btn-primary">Yeni Grup Girişi</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>
